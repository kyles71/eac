name: Deploy dev branch

on:
  push:
    branches: [ dev ]
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    secrets: inherit
    with:
      environment: dev
      concurrency_group: dev
